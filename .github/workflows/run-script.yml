name: Ping URLs Every 10 Minutes

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:        # Allows manual trigger too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call URLs
        run: |
          curl -s https://florist-ke.onrender.com
          curl -s https://cybernet-sro7.onrender.com
          curl -s https://comas.onrender.com
          curl -s https://mindhealthconsultancy.com
          curl -s https://interunihackathon.co.ke
